( function( api ) {

	// Extends our custom "para" section.
	api.sectionConstructor['para'] = api.Section.extend( {

		// No events for this type of section.
		attachEvents: function () {},

		// Always make the section active.
		isContextuallyActive: function () {
			return true;
		}
	} );

} )( wp.customize );